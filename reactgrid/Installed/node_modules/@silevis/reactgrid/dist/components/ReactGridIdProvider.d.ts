import { default as React, FC } from 'react';

interface ReactGridIdContextProps {
    id: string;
    children: React.ReactNode;
}
export declare const ReactGridIdProvider: FC<ReactGridIdContextProps>;
export declare const useReactGridId: () => string;
export {};

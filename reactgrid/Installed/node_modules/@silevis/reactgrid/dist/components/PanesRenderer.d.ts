import { FC } from 'react';
import { CellMap } from '../types/InternalModel';
import { Column, Row } from '../types/PublicModel';

interface PanesRendererProps {
    rows: Row[];
    columns: Column[];
    cells: CellMap;
    stickyTopRows: number;
    stickyBottomRows: number;
    stickyLeftColumns: number;
    stickyRightColumns: number;
}
declare const PanesRenderer: FC<PanesRendererProps>;
export default PanesRenderer;

import { default as React, FC, PropsWithChildren } from 'react';

interface GridWrapperProps {
    reactGridId: string;
    style?: React.CSSProperties;
}
declare const GridWrapper: FC<PropsWithChildren<GridWrapperProps>>;
export default GridWrapper;

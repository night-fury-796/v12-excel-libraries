import { FocusedCell } from '../types/InternalModel.ts';
import { ReactGridStore } from '../types/ReactGridStore.ts';

export declare const moveFocusUp: (store: ReactGridStore, currentFocus: FocusedCell) => Partial<ReactGridStore>;
export declare const moveFocusRight: (store: ReactGridStore, currentFocus: FocusedCell) => ReactGridStore | {
    selectedArea: {
        startRowIdx: number;
        endRowIdx: number;
        startColIdx: number;
        endColIdx: number;
    };
};
export declare const moveFocusDown: (store: ReactGridStore, currentFocus: FocusedCell) => ReactGridStore | {
    selectedArea: {
        startRowIdx: number;
        endRowIdx: number;
        startColIdx: number;
        endColIdx: number;
    };
};
export declare const moveFocusLeft: (store: ReactGridStore, currentFocus: FocusedCell) => ReactGridStore | {
    selectedArea: {
        startRowIdx: number;
        endRowIdx: number;
        startColIdx: number;
        endColIdx: number;
    };
};
export declare const moveFocusInsideSelectedRange: (store: ReactGridStore, currentFocus: FocusedCell, direction: "up" | "right" | "down" | "left") => ReactGridStore;
export declare const moveFocusPageUp: (store: ReactGridStore, currentFocus: FocusedCell) => ReactGridStore;

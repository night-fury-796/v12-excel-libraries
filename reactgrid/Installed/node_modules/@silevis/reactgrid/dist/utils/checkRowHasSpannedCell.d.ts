import { SpanMember } from '../types/InternalModel';
import { ReactGridStore } from '../types/ReactGridStore';

export declare const checkRowHasSpannedCell: (store: ReactGridStore, rowIndex: number) => SpanMember | undefined;

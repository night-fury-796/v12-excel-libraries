import { ReactGridStore } from '../types/ReactGridStore.ts';
import { default as React } from 'react';

type HandleKeyDownConfig = {
    moveHorizontallyOnEnter: boolean;
};
export declare const handleKeyDown: (event: React.KeyboardEvent<HTMLDivElement>, store: ReactGridStore, config?: HandleKeyDownConfig) => Partial<ReactGridStore>;
export {};

import { ReactGridStore } from '../types/ReactGridStore';

export declare const getLastColumnMetrics: (store: ReactGridStore) => {
    lastColumnRelativeffsetLeft: number;
    lastColumnClientOffsetLeft: number;
    lastColumnCellWidth: number;
};

import { PaneName } from '../types/InternalModel';
import { ReactGridStore } from '../types/ReactGridStore';

export declare const isPaneExists: (store: ReactGridStore, panePosition: PaneName) => boolean;

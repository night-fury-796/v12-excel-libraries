import { IndexedLocation } from '../types/InternalModel';
import { ReactGridStore } from '../types/ReactGridStore';

export declare const getCellPaneOverlap: (store: ReactGridStore, destinationCellIdx: IndexedLocation, panePosition: "Left" | "Right" | "TopCenter" | "BottomCenter") => number | undefined;

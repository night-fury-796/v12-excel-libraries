import { ReactGridStore } from '../types/ReactGridStore';

export declare const getLastRowMetrics: (store: ReactGridStore) => {
    lastRowRelativeOffsetTop: number;
    lastRowClientOffsetTop: number;
    lastRowHeight: number;
};

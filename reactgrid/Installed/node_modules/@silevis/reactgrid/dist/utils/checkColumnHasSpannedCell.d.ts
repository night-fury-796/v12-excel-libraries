import { SpanMember } from '../types/InternalModel';
import { ReactGridStore } from '../types/ReactGridStore';

export declare const checkColumnHasSpannedCell: (store: ReactGridStore, colIndex: number) => SpanMember | undefined;

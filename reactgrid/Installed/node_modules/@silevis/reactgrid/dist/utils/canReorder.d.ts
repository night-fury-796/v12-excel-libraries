import { Cell } from '../main';
import { ReactGridStore } from '../types/ReactGridStore';

export declare function canReorder(store: ReactGridStore, reorderType: "row" | "column", clickedCell: Cell): boolean;

import { IndexedLocation } from '../types/InternalModel.ts';
import { Cell } from '../types/PublicModel.ts';
import { ReactGridStore } from '../types/ReactGridStore.ts';

interface CellAndLocation extends IndexedLocation {
    cell?: Cell;
}
export declare const getLocationFromClient: (store: ReactGridStore, clientX: number, clientY: number) => CellAndLocation;
export {};

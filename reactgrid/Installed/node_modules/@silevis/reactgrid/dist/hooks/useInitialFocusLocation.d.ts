import { ReactGridProps } from '../types/PublicModel';
import { ReactGridStore } from '../types/ReactGridStore';

export declare const useInitialFocusLocation: (store: ReactGridStore, rgProps: Partial<ReactGridProps>, devEnvironment: boolean) => void;

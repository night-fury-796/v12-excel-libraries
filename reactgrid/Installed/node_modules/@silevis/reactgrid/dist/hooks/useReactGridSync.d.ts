import { CellMatrix } from '../types/CellMatrix';
import { ReactGridStore, ReactGridStoreProps } from '../types/ReactGridStore';

export declare const useReactGridSync: (store: ReactGridStore, cellMatrix: CellMatrix, rgProps: Partial<ReactGridStoreProps>) => void;

import { ReactGridProps } from '../types/PublicModel';
import { ReactGridStore } from '../types/ReactGridStore';

export declare const useInitialSelectedRange: (store: ReactGridStore, rgProps: Partial<ReactGridProps>, devEnvironment: boolean) => void;

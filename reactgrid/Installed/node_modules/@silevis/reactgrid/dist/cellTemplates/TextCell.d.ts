import { FC } from 'react';

interface TextCellProps {
    text: string;
    onTextChanged: (newText: string) => void;
    style?: React.CSSProperties;
}
export declare const TextCell: FC<TextCellProps>;
export {};
